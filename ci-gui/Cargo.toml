[package]
name = "ci-gui"
version = "0.2.0"
edition = "2024"

[dependencies]
ci-lisp = { path = "../ci-lisp" }
ci-cli = { path = "../ci-cli" }

eframe = { version = "0.32.0", default-features = false, features = [
  "default_fonts",
  "wayland",
  "glow"
]}

clap.workspace = true
